import { API_BASE_URL as GLOBAL_API, FILE_BASE_URL as GLOBAL_FILE, getFileUrl as GLOBAL_GET_FILE_URL } from '../../apiConfig';

export const API_BASE_URL = GLOBAL_API;
export const FILE_BASE_URL = GLOBAL_FILE;
export const getFileUrl = GLOBAL_GET_FILE_URL;
